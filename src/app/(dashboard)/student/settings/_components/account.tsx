import { ProfileForm } from "@/components/forms/student/profile-form";
import React from "react";

type Props = {};

const Account = (props: Props) => {
  return <ProfileForm />;
};

export default Account;
